int input=0;
int count=0;
double sum=0;

do {
    cout << "Integer? ";
    cin >> input;
    if(input%2==0 && input!=0){
        sum+=input;
        count++;
    }
    }while(input!=0);

cout << "Average: " << sum/count;